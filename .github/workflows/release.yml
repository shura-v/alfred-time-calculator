name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          run_install: true

      - name: Build Alfred Workflow
        run: pnpm run build:workflow

      - name: 🕵️ Check workflow file exists
        run: |
          echo "Looking for dist-release/time-calculator.alfredworkflow"
          ls -lh dist-release || echo "❌ No dist-release folder"
          ls -lh dist-release/time-calculator.alfredworkflow || echo "❌ File not found"

      - name: Run semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx semantic-release
